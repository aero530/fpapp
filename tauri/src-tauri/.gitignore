# ignore the main target folder
/target/

# ignore all other target folders
target/

# dont wanna save Wix to git either
WixTools

*.csv

# This app is deprecated and excluded from the workspace; it generates its own
# lockfile when built ad hoc. No reproducibility requirement, so don't track it.
/Cargo.lock